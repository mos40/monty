#include "monty.h"
/**
 * f_stack - prints then display the top of the f stack
 * @head: stack head to the ptr .
 * @counter: line_numbr .
 * Return: no return val
*/
void f_stack(stack_t **head, unsigned int counter)
{
	(void)head;
	(void)counter;
	bus.lifi = 0;
}
